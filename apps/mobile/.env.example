# URL da API vista pelo celular. Rodando no Expo Go, use o IP da sua maquina na
# rede local (nao `localhost`, que no celular aponta para o proprio aparelho).
# Descubra com: ip addr | grep "inet " (Linux) ou ipconfig getifaddr en0 (macOS).
EXPO_PUBLIC_API_URL=http://192.168.0.10:3333
